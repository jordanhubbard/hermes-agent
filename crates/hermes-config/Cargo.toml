[package]
name = "hermes-config"
version = "0.1.0"
edition = "2021"
rust-version = "1.70"
description = "Rust config and profile path semantics for Hermes Agent"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
